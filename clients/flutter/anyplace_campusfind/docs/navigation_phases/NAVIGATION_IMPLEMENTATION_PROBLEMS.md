# Navigation Implementation — Cumulative Problem Log

Live log for the Phase 0→16 execution run. Only real problems encountered during execution are recorded.

| ID | Phase | Location | Symptom | Root cause | Impact | Resolution | Verification | Status | Related plan item |
|---|---|---|---|---|---|---|---|---|---|
| NAV-P001 | 0 | `test/navigation_baseline_characterization_test.dart` (authoring) | Write aborted mid-JSON twice; first concatenation placed `_FakeScope` inside `main()` → syntax errors | Write-tool payload size limit + assembly mistake when concatenating parts | Authoring friction only | Split content, relocated class above `main()`, re-analyzed clean | File analyzes clean; suite passes | RESOLVED | Ph0 task 3 |
| NAV-P002 | 0 | characterization fakes | Compile error: final field unassigned; draft referenced non-existent override machinery | Over-simplified ctor; undefined helper references | Blocked test load | Inline default; single stub serving replacement geometry | Suite compiles, 10/10 pass | RESOLVED | Ph0 task 3 |
| NAV-P003 | 1 | navigation_controller.dart | 6 compile errors after destination-field removal (stale `_destinationSpace/_destinationPuid` reads); one wrong-site fix attempt on `_isOutsideBuilding` immediately reverted | Mechanical sweep missed internal read sites; sed matched look-alike text | Compile break | Migrated all reads to session getters; preserved original semantics at wrong site | Full suite green | RESOLVED | Ph1 wiring |
| NAV-P004 | 1 | session_identity_test.dart | Ctor name mismatch (`Scope` vs `_Scope`); `same(_replacement())` compared distinct instances | Authoring slips | 2 test failures | Renamed; hoisted single instance for identity compare | 5/5 pass | RESOLVED | Ph1 tests |
| NAV-P005 | 3→4 | arrival/ui/state_machine/floor_transition test fakes | PowerShell splice scripts corrupted four files (insertion at wrong index; literal `\r\n`; `$(` interpolation invalid in Dart) → up to 222 analyzer issues, load failures | Batch string ops vs mixed line endings + PS escaping rules | Blocked builds/tests | git checkout restore of the four files + Edit-tool re-application + `${poi.puid}` fix | analyze ≤ baseline; suites green | RESOLVED | Ph3/Ph4 fake updates |
| NAV-P006 | 4/5 | handoff/rerouting/session fixtures | Fixture-level interactions with real contract: dest POI co-located with GPS fired early ARRIVAL; arbiter scope-confirmation lag consumed 3 estimates before drift detection; 0.0005° hops (~56 m) tripped indoor outlier guard; exit dwell needs confirming tick beyond count | Tests must respect verified pipeline semantics (hold/suppression/outlier/scope-lag) | Test-only churn | Adjusted fixture geometry/cadence per contract; documented in phase reports | All targeted suites green | RESOLVED | Ph8/Ph9/Ph13 fixtures |
| NAV-P007 | 14 | route_store/_RecordingScope adopt path | Debug assert 'reentrant adoptNavigatedRoute detected' fired across 13 reroute-dependent tests | Batch replace rewrote the wrapper's own body into a recursive self-call | Broad test failure wave | Wrapper body restored to call `_spaceScope.adoptNavigatedRoute` | Full suite green; guard active | RESOLVED | Ph14 task 4 |
| NAV-P008 | 15 | NC event emitter + docs lint | PowerShell here-string mangled `$var` detail strings into invalid escapes (`'\->\'`); doc-comment angle brackets triggered unintended_html ×6 | PS escaping rules; analyzer doc lint | Compile break / noise | Hand-fixed strings via Edit; reworded format block to plain key=value tokens | analyze 29 issues; log-contract test green | RESOLVED | Ph15 tasks 4–5 |
| NAV-P009 | 16 | integration_journey_test.dart | Missing imports/ctor name; golden journey initially counted repo-side requests the scope stub never made; arrival-stage hop exceeded outlier window so anchor never confirmed | Same authoring patterns as P004/P006 | Blocked new suite | Added imports/rename; counter moved onto scope stub; shortened hops and aligned route endpoint to emitted position | 2/2 pass incl. PERF line | RESOLVED | Ph16 item 1/4 |
